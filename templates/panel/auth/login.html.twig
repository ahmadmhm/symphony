{% extends 'panel/layouts/master.html.twig' %}

{% block content %}
    <form action="{{ path('panel_login_show') }}">
        <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
        <button>Login</button>
    </form>

    {% block page_contents %}{% endblock %}
{% endblock %}